# Run linting
npm run lint

# Run unit tests for changed files
npm run test:changed

# E2E tests are now optional - run manually when needed
# Uncomment the lines below if you want automatic E2E testing on commit
# if git diff --cached --name-only | grep -E "(theme|ThemeToggle|layout\.tsx|globals\.css)" > /dev/null; then
#   echo "Theme-related files changed, running theme toggle tests..."
#   npm run test:e2e:theme
# fi